import { FiltropeliculaPipe } from './filtropelicula.pipe';

describe('FiltropeliculaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltropeliculaPipe();
    expect(pipe).toBeTruthy();
  });
});
